# Preferred: full connection string
DATABASE_URL=postgres://postgres:postgres@localhost:5432/klima

# Or use individual PG vars
# PGHOST=localhost
# PGPORT=5432
# PGUSER=postgres
# PGPASSWORD=postgres
# PGDATABASE=klima

PORT=4000


# Default seeded admin for /auth/login during create-db
ADMIN_EMAIL=admin@klima.local
ADMIN_PASSWORD=admin123

# SMTP configuration for sending emails
SMTP_HOST=smtp.izkat.com
SMTP_PORT=587
SMTP_USER=xiroce8125@izkat.com
SMTP_PASS=your_smtp_password_here
SMTP_FROM=xiroce8125@izkat.com

# Recipient for historical requests
HISTORICAL_REQUEST_RECIPIENT=22-09374@g.batstate-u.edu.ph
